[profile.release]
lto = true
strip = true
panic = "abort"
opt-level = 3
codegen-units = 1

[package]
name = "pdu_exporter"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = { version = "0.8.4", features = ["tokio"] }
tokio = { version = "1.46.1", features = ["full"] }
